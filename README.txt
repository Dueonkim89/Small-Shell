Double check to see if there is a Makefile for C.

In terminal use command: gcc --std=gnu99 -o smallsh smallsh.c

Once executable is created use command: smallsh

This project is a lightweight shell for the linux.

NOTE: Does not support pipe operator.
